/**
 * Coherence System Module
 *
 * Validates alignment across all orchestrator components.
 * The "spec-driven organization" spine.
 */

export {
  CoherenceService,
  type AlignmentDomain,
  type IssueSeverity,
  type IssueStatus,
  type CoherenceIssue,
  type DomainValidation,
  type CoherenceReport,
  type CoherenceRecommendation,
  type CoherenceRule,
  type CoherenceServiceOptions,
} from './CoherenceService.js';
